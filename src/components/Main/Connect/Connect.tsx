// import { firestore, auth } from "../../../firebase/fireInstance"

const Connect = () => {
    return (
        <div>
            Connect to other users! Not yet though.
        </div>
    )
}

export default Connect
